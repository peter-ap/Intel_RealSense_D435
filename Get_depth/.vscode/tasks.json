{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "g++",
            "args": ["-g", "main.cpp", "-o", "grab_depth_intel_realsense", "-lrealsense2", "-lopencv_core", "-lopencv_highgui"
            ],
            "group":{
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}